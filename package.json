{
  "name": "bandacious-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "scripts": {
    "build": "esbuild main.jsx --bundle --format=esm --outfile=dist/main.js --loader:.jsx=jsx && mkdir -p dist && cp index.html dist/index.html && sed -i 's/\\.\\/main\\.jsx/\\.\\/main\\.js/' dist/index.html"
  }
}
